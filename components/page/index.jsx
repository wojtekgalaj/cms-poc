import React from 'react';

const Page = ({page}) => {

  // get page elements
  // instantiate a compponent for each element
  // push it to elements
  var pageElements = [];
  return (
    <div>
    </div>
  )
};
